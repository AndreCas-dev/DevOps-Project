# Node Exporter Web Configuration
# Reference: https://github.com/prometheus/exporter-toolkit/blob/master/docs/web-configuration.md

# TLS configuration (disabled by default)
# tls_server_config:
#   cert_file: /etc/node-exporter/cert.pem
#   key_file: /etc/node-exporter/key.pem

# Basic authentication (disabled by default)
# basic_auth_users:
#   admin: $2y$10$...  # bcrypt hash of password
